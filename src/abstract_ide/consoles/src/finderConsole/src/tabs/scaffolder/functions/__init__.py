# Scaffolder methods, bound onto the window instance by
# abstract_utilities.import_utils.initFuncs (dev mode scans this package and
# binds every module-level `def name(self, ...)` as a method).
#
# Recovered 2026-07-11 from the orphaned __pycache__ bytecode
# (build_ui/utils .cpython-313.pyc) after the migration lost the source.
# Behaviour reconstructed from the compiled method blueprints.
